version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    container_name: acuchat_db
    environment:
      POSTGRES_USER: claudey_db
      POSTGRES_PASSWORD: claudeyy123